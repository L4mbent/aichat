server {
    listen 80;
    server_name asuna-bot.your-domain.com;

    client_max_body_size 10M;

    location /health {
        proxy_pass http://127.0.0.1:8080/health;
        proxy_set_header Host $host;
    }
}
